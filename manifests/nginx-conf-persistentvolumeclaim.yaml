apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: nginx-conf
  name: nginx-conf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
