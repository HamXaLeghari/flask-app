apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: mariadb-data
  name: mariadb-data
spec:
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
     storage: 10Gi
  storageClassName: local-path


